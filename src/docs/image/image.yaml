tags:
  - Image Filter
parameters:


  - in: query
    name: image_url
    type: string
    required: true
    description: The image link to processed


responses:
  200:
    description: Endpoint returns  the processed image.

  500:
    description: Internal Server Error.
